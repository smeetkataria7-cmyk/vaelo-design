import { ComingSoon } from "@/components/app/coming-soon";

export default function CreativesPage() {
  return (
    <ComingSoon
      title="Creatives"
      subtitle="Asset approval 2.0 — versions, pinned comments, history"
      phase="Phase 4 — Delivery & stickiness"
    />
  );
}
